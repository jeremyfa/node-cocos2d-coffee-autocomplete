
# cc.StopGrid action.
class StopGrid extends ActionInstant

    # Please use cc.stopGrid instead Allocates and initializes the action
    @StopGrid.create = {}

    # Constructor
    # @return [StopGrid]
    constructor: ->

    # called before the action start.
    # @param [Node] target
    startWithTarget: (target) ->

