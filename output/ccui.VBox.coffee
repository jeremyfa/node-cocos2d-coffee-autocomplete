
# The vertical box of Cocos UI.
class ccui.VBox extends ccui.Layout

    # Constructor
    # @return [ccui.VBox]
    constructor: ->

    # Creates a VBox
    # @param [Size] size
    # @return [ccui.VBox]
    @create: (size) ->

    # Initializes a VBox.
    # @return [boolean]
    init: ->

    # Initializes a VBox with size.
    # @param [Size] size
    # @return [boolean]
    initWithSize: (size) ->

