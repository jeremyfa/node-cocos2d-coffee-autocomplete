
# OpenGL projection protocol
class DirectorDelegate extends Class

    # Constructor
    # @return [DirectorDelegate]
    constructor: ->

    # Called by CCDirector when the projection is updated, and "custom" projection is used
    updateProjection: ->

