
# MenuPassive: The menu passive ui component
class MenuPassive extends Layer

    # Constructor
    # @return [MenuPassive]
    constructor: ->

    # align items horizontally
    alignItemsHorizontally: ->

    # align items horizontally with padding
    # @param padding
    alignItemsHorizontallyWithPadding: (padding) ->

    # align items in rows of columns
    # @param columns
    alignItemsInColumns: (columns) ->

    # align items in columns of rows
    # @param rows
    alignItemsInRows: (rows) ->

    # align items vertically
    alignItemsVertically: ->

    # align items vertically with padding
    # @param padding
    alignItemsVerticallyWithPadding: (padding) ->

    # creates an empty CCMenu
    # @param item
    @create: (item) ->

    # creates a CCMenu with it's item, then use addChild() to add other items.
    # @param item
    @createWithItem: (item) ->

    # Color: conforms with CCRGBAProtocol protocol
    getColor: ->

    # Opacity: conforms with CCRGBAProtocol protocol
    getOpacity: ->

    # initializes a CCMenu with it's items
    # @param item
    # @param args
    initWithItems: (item, args) ->

