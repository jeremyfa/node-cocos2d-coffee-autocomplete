
# cc.StandardTouchHandler It forwardes each event to the delegate.
class StandardTouchHandler extends TouchHandler

    # Constructor
    # @return [StandardTouchHandler]
    constructor: ->

    # Create a TouchHandler with a delegate and a priority
    # @param [Object] delegate
    # @param [Number] priority
    # @return [StandardTouchHandler]
    @create: (delegate, priority) ->

    # Initializes a TouchHandler with a delegate and a priority
    # @param [TouchDelegate] delegate
    # @param [Number] priority
    # @return [Boolean]
    initWithDelegate: (delegate, priority) ->

